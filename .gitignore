# Data — commit the demo dataset + the scored output so the app/deploy work
# out of the box; ignore bulky intermediates (regenerate with run_all.py).
data/raw/*.csv
!data/raw/prescribers.csv
data/processed/*
!data/processed/.gitkeep
!data/processed/scored.csv

# Personal prep doc — never publish
INTERVIEW_STUDY_GUIDE.md

# python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# os
.DS_Store
